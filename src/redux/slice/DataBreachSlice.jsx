const { createSlice } = require("@reduxjs/toolkit");

export const DataBreachSlice = createSlice({
    name : 'dataBreach',
    initialState : [],
    reducers : {}
})
export const {} = DataBreachSlice.actions;
export default DataBreachSlice.reducer